# Roadmap

## v0.2

- integrate a real GGUF/llama.cpp local backend implementation
- add pluggable SIMD kernels with runtime dispatch validation
- add richer `/metrics` counters and histograms
- extend scenario DSL and replay artifact persistence
- introduce config file loading with schema validation

## v0.3+

- fleet-level orchestration APIs
- policy packs
- enterprise observability adapters
