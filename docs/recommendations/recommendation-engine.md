# Recommendation Engine

- Deterministic rule-based output by default.
- Produces profile, provider order, task model mapping, quantization, preload plan, privacy suggestion, cost controls, explanation, confidence, warnings.
- Optional explanation expansion through `SAWYER_AI_RECOMMENDATIONS=true`.
- AI explanation mode cannot auto-apply config changes.
