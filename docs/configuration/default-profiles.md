# Default Profiles

Versioned templates in `profiles/`:
- local-safe
- balanced
- performance
- mobile-edge
- cost-saver
- developer

Local-safe keeps cloud fallback disabled by default and enforces strict audit semantics.
